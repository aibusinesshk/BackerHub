/**
 * Re-export JSON-LD structured data components from the canonical location.
 * Import from here or from '@/components/seo/json-ld' — both work.
 */
export {
  OrganizationJsonLd,
  WebSiteJsonLd,
  BreadcrumbJsonLd,
  FAQPageJsonLd,
  PlayerProductJsonLd,
} from '@/components/seo/json-ld';
